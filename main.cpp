#include <iostream>
#include <stdlib.h>

using namespace std;

int main() {
    int base, altura;
    cin >> base;
    cin >> altura;

    cout << (base*altura)/2 << endl;
    system("pause");
    return 0;
}